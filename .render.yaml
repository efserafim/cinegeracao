services:
  - type: web
    name: cinegeracao
    env: node
    branch: main
    buildCommand: npm install && npm install --prefix backend && npm install --prefix frontend && npm --prefix backend run prisma:generate && npm --prefix backend run prisma:migrate:deploy-if-db
    startCommand: npm run start --prefix backend
