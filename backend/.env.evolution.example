# Example env file for running Evolution API locally (do NOT commit secrets)
PORT=8080
# Authentication key from your Evolution API deployment (admin key)
AUTHENTICATION_API_KEY=<SEU_AUTHENTICATION_API_KEY>
# Public URL where Evolution API is reachable (ex: https://evolution-api-b4r0.onrender.com)
SERVER_URL=http://localhost:8080
